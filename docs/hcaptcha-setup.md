# Налаштування hCaptcha для Image Challenge

## ✅ Переваги hCaptcha над reCAPTCHA v2

**hCaptcha ЗАВЖДИ показує image challenge** (вибір картинок з автобусами, сходинками, світлофорами тощо), на відміну від Google reCAPTCHA v2, який вирішує автоматично.

## 🚀 Швидкий старт

### 0. Встановлення пакета

Спочатку встановіть офіційну бібліотеку hCaptcha для React:

```bash
pnpm add @hcaptcha/react-hcaptcha
```

### 1. Реєстрація та отримання ключів

1. Перейдіть на https://www.hcaptcha.com/
2. Створіть безкоштовний акаунт
3. Створіть новий сайт в Dashboard (вкладка **Sites**)
4. Скопіюйте **Site Key** (він відображається на сторінці сайту)

#### Де знайти Secret Key:

**Secret Key знаходиться в іншому місці!**

1. Перейдіть на https://dashboard.hcaptcha.com/settings
2. Або в Dashboard натисніть на **Settings** (Налаштування) в меню
3. У розділі **Secret Key** ви побачите ваш Secret Key
4. Якщо Secret Key не видно або ви його втратили:
   - Натисніть кнопку **"Generate New Secret"** (Згенерувати новий Secret)
   - ⚠️ **Увага**: Secret Key один для всього акаунту (не для кожного сайту окремо)

### 2. Налаштування змінних середовища

Додайте в `.env.local`:

```env
# hCaptcha keys
NEXT_PUBLIC_HCAPTCHA_SITE_KEY=your_site_key_here
HCAPTCHA_SECRET_KEY=your_secret_key_here
```

### 3. Налаштування доменів

В hCaptcha Dashboard:
1. Перейдіть до налаштувань вашого сайту
2. Додайте домени, на яких буде використовуватися hCaptcha:
   - Для розробки: `localhost`, `127.0.0.1`
   - Для продакшену: `coldi.ai`, `www.coldi.ai`
3. Збережіть зміни

### 4. Перезапустіть dev сервер

```bash
pnpm dev
```

## 📝 Поточна конфігурація

В `src/shared/lib/captcha-config.ts`:

```typescript
export const HCAPTCHA_ENABLED = true;  // Увімкнено
export const RECAPTCHA_ENABLED = false; // Вимкнено
export const TURNSTILE_ENABLED = false; // Вимкнено
```

## 🔧 Технічні деталі

Проєкт використовує офіційну бібліотеку `@hcaptcha/react-hcaptcha` від hCaptcha:
- GitHub: https://github.com/hCaptcha/react-hcaptcha
- Документація: https://docs.hcaptcha.com/

Компонент `HCaptcha.tsx` є обгорткою над офіційною бібліотекою для зручності використання в проєкті.

## 🎯 Як це працює

1. **hCaptcha завжди показує image challenge** - користувач обов'язково має вибрати правильні картинки
2. **Безкоштовно** - hCaptcha безкоштовна для більшості випадків використання
3. **Приватність** - hCaptcha не збирає дані користувачів для реклами (на відміну від reCAPTCHA)

## 🔄 Повернення до reCAPTCHA

Якщо потрібно повернутися до reCAPTCHA:

1. В `src/shared/lib/captcha-config.ts`:
   ```typescript
   export const HCAPTCHA_ENABLED = false;
   export const RECAPTCHA_ENABLED = true;
   ```

2. Перезапустіть сервер

## 📚 Документація

- Офіційна документація: https://docs.hcaptcha.com/
- Dashboard: https://dashboard.hcaptcha.com/
