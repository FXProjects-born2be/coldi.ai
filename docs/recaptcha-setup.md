# Налаштування Google reCAPTCHA v2 для Image Challenge

## ⚠️ Важлива інформація

**Google reCAPTCHA v2 не дозволяє примусово завжди показувати image selection challenge.** Google автоматично визначає, коли показувати challenge на основі:
- Ризику сесії
- Поведінки користувача
- IP-адреси та браузера
- Налаштувань безпеки в Google Console

## 🎯 Як збільшити ймовірність показу Image Challenge

### 1. Налаштування в Google reCAPTCHA Admin Console (ОБОВ'ЯЗКОВО!)

1. Перейдіть на https://www.google.com/recaptcha/admin
2. Виберіть ваш сайт (або створіть новий)
3. Знайдіть налаштування **"Security Preference"** (Слайдер безпеки)
4. **Перемістіть слайдер до "Most secure" (Найбезпечніше)** - це найважливіше!
5. Збережіть зміни

**Це критично важливо** - без цього налаштування image challenge буде з'являтися рідко або взагалі не з'являтися.

### 2. Використання Fallback Mode (для тестування)

Додайте в `.env.local`:
```
NEXT_PUBLIC_RECAPTCHA_FALLBACK=true
```

Це може допомогти частіше тригерити challenge, але не гарантує завжди показувати його.

### 2. Тестування Image Challenge

Для тестування image challenge використовуйте:

- **Incognito/Приватний режим браузера** - зазвичай тригерить challenge
- **VPN або інший IP** - новий IP може спровокувати challenge
- **Багаторазові запити** - повторні запити з того ж IP можуть спровокувати challenge
- **Блокування Google cookies** - може збільшити ймовірність challenge

### 3. Альтернативні рішення

Якщо потрібно **завжди** показувати image challenge:

#### Варіант A: reCAPTCHA Enterprise (Платна послуга)
- Використовуйте **Policy-based Challenge Keys**
- Дозволяє налаштувати автоматичний показ challenge на основі score thresholds
- Більше контролю над поведінкою капчі

#### Варіант B: Інші сервіси CAPTCHA
- hCaptcha - має опцію завжди показувати challenge
- FunCaptcha - завжди показує image challenge
- Cloudflare Turnstile - безкоштовна альтернатива (але без image challenge)

## Поточна реалізація

Поточна реалізація використовує стандартний reCAPTCHA v2 з налаштуваннями:
- `size: 'normal'` - показує checkbox
- `theme: 'light'` - світла тема
- Image challenge з'являється автоматично після кліку на checkbox, якщо Google визначить це необхідним

## Рекомендації

1. **Для продакшену**: Залиште поточну реалізацію та налаштуйте Security Preference на максимум
2. **Для тестування**: Використовуйте incognito режим
3. **Якщо потрібен завжди challenge**: Розгляньте reCAPTCHA Enterprise або інші сервіси
